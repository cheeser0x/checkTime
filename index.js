// Log the server's current date and time
function logServerTime() {
  const now = new Date();
  console.log("Server's current date and time:", now.toString());
}

// Call the function to log the time
logServerTime();
